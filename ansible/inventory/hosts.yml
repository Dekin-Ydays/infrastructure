---
all:
  children:
    frontend:
      hosts:
        vm1_frontend:
          ansible_host: 192.168.1.10  # Replace with actual IP
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    backend:
      hosts:
        vm2_backend:
          ansible_host: 192.168.1.11  # Replace with actual IP
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    database:
      hosts:
        vm3_database:
          ansible_host: 192.168.1.12  # Replace with actual IP
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    storage:
      hosts:
        vm4_storage:
          ansible_host: 192.168.1.13  # Replace with actual IP
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
