---
# Database-specific variables
# ===========================

# Allowed ports for UFW (PostgreSQL only from backend)
ufw_allowed_ports:
  - { port: 22, proto: tcp, from: any }
  - { port: 5432, proto: tcp, from: "{{ vm2_backend_ip }}" }
